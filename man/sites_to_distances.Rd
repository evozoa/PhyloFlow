% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sites_to_distances.R
\name{sites_to_distances}
\alias{sites_to_distances}
\title{Compute pairwise stream network distances between sample sites}
\usage{
sites_to_distances(
  sites,
  site_id_col = "sample_id",
  lat_col = "lat",
  lon_col = "lon",
  network = NULL,
  buffer_km = 50,
  min_stream_order = 3,
  length_col = "lengthkm",
  weight_col = NULL,
  format = "matrix"
)
}
\arguments{
\item{sites}{A data frame with one row per sample site.}

\item{site_id_col}{Character. Column name for sample IDs. Default
\code{"sample_id"}.}

\item{lat_col}{Character. Column name for latitude (decimal degrees, WGS84).
Default \code{"lat"}.}

\item{lon_col}{Character. Column name for longitude (decimal degrees,
WGS84). Default \code{"lon"}.}

\item{network}{An \code{sf} object of stream network line segments. If
\code{NULL}, NHD data is fetched automatically.}

\item{buffer_km}{Numeric. Buffer (km) around the bounding box of all sites
for NHD download. Default 50.}

\item{min_stream_order}{Integer. Minimum NHD stream order to snap samples
to. Default 3.}

\item{length_col}{Character. Column name for segment lengths. Default
\code{"lengthkm"}. Set to \code{NULL} to compute from geometry in metres.}

\item{weight_col}{Character or \code{NULL}. Optional NHD column to use as a
resistance weight. Segment lengths are multiplied by this value before
summing path distances. For example, \code{"slope"} weights distances by
channel gradient. Default \code{NULL} (unweighted stream distance).}

\item{format}{Character. \code{"matrix"} (default) returns a symmetric
matrix; \code{"dist"} returns an object of class \code{dist}.}
}
\value{
A symmetric distance matrix (or \code{dist} object) of pairwise
stream network distances, with rows and columns named by sample ID.
}
\description{
Computes a matrix of pairwise distances between sample collection sites
measured along the stream network. Suitable for use in Mantel tests and
other isolation-by-distance analyses with nuclear markers.
}
\details{
Optionally, segment lengths can be weighted by any NHD attribute (e.g.
\code{"slope"} or \code{"qa_ma"} for mean annual discharge) to produce
resistance-based distances.
}
