% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_to_tree.R
\name{network_to_tree}
\alias{network_to_tree}
\title{Convert an upstream stream network to a phylogenetic tree}
\usage{
network_to_tree(
  upstream_network,
  segment_col = "comid",
  length_col = NULL,
  from_col = "fromnode",
  to_col = "tonode",
  format = "newick",
  file = NULL
)
}
\arguments{
\item{upstream_network}{An \code{sf} object of upstream stream segments as
returned by \code{extract_upstream}.}

\item{segment_col}{Character. Column uniquely identifying each segment.
Default \code{"comid"} for NHD data.}

\item{length_col}{Character or \code{NULL}. Column for segment length. If
\code{NULL}, lengths are computed from geometry in metres. For NHD data
use \code{"lengthkm"} for lengths in kilometres.}

\item{from_col}{Character. Upstream node column for non-NHD networks.
Default \code{"fromnode"}.}

\item{to_col}{Character. Downstream node column for non-NHD networks.
Default \code{"tonode"}.}

\item{format}{Character. \code{"newick"} (default) or \code{"nexus"}.}

\item{file}{Character or \code{NULL}. Output file path. If \code{NULL} the
tree is returned without writing to disk.}
}
\value{
An \code{ape} \code{phylo} object.
}
\description{
Converts a directed stream network (as returned by \code{extract_upstream})
into a rooted phylogenetic tree. Each stream segment becomes a branch, with
length equal to segment length. For NHD data, topology is derived from
\code{hydroseq}/\code{dnhydroseq}, which are consistent across VPU
boundaries. For user-supplied networks, topology is derived from node ID
columns.
}
